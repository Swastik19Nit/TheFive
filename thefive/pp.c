void selection sort(int array[],int n)
{
    int index;
    for(int i=0;i<n-1;i++)
    {
        index=i;
        for(int j = i + 1; j < n; j++)
        {
            if array[j]<array[index]
                index=j;
        }
        int temp = array[]

    }
}